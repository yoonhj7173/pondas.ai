# Implementation Plan v3

> Updated 2026-06-13 against PRD v2 + tech-design v3 (decision-log D1–D44). Two tracks per D28: **Track 1 (app)** and **Track 2 (execution engine)** — Track 2 items are independent of Track 1 frontend and can proceed in parallel after item 5. Each item is a single testable unit; check off as completed. **Frontend (19–26) recreates `claude-design-handoff/product/` in our stack (Pixi world + React HUD) — that README is the visual source of truth (D36); landing/blog moved to item 27, out of current scope.**
>
> **Build order (unchanged, deliberate):** backend data layer + services (5–13) → execution engine (14–18, parallelizable after 5) → frontend (19–26) → deferred landing/blog (27). Frontend follows backend because every screen consumes a real API. **Feature tests must verify real behavior against live infra (Postgres/Redis via docker-compose; real E2B for Track 2), not just builds.**

## v1 foundations — what actually survives vs gets reworked

The backend was built against the **v1 spec** (`clusters`/`units`). Auditing it against v3 (D5–D44):

**Truly reusable (stack-level, ~550 LOC — keep):**
- [x] **App skeleton** (was item 2) — FastAPI, `config.py`, `db.py`, `/health`+`/ready`, structured logging. Stack-level, unaffected by the data-model change.
- [x] **Auth + tenancy** (was item 4) — Clerk JWT, `require_user`, `TenantScope` (`auth.py`). Item 5/6 extends scoping to projects.
- [x] **Continuation mechanism** (was item 1) — the re-enqueue + `AWAITING_INPUT` sentinel logic in `crews/base.py` (9/9 tests). The *mechanism* is reused by items 10–11/16; the v1 `factory.py` that builds from `units` rows is rewritten at item 10.

**v1 code replaced starting at item 5 (~680 LOC — do NOT treat as done):**
- `models.py` (Cluster/Unit/Task/Notification/Config v1), `schemas.py`, the v1 Alembic migration, `seed.py` v1 → **item 5** (clean v3 reset migration — see note).
- `routers/units.py`, `routers/map.py` → **items 6–7** (projects/teams/agents/edges/map).
- `crews/factory.py` (v1) → **item 10**. Tests `test_map_units`, `test_schema_seed` → rewritten alongside.

> **Item 5 migration note (no prod data):** since v1 has only dev seed data, do **not** stack a "replace" migration on top of the v1 one — **reset to a single clean v3 initial migration** (drop the v1 `adca1261107f`, author one v3 `initial_schema`). Cleaner history, no dead intermediate state.

## Track 1 — App backend

- [x] 5. **Schema v3 + template seed** — Alembic migration replacing `clusters`/`units` with the v3 model (tech-design §5): `user_profiles`, `projects` (`paused`, `sandbox_id/status`), templates (`engine`, `default_tier`) + `edge_templates`, `teams` (`room_x/room_y`, D39)/`agents` (`model_tier`), `edges` (**unique index on `from_agent_id` — one outgoing edge max, D38**), `goals`, `tasks` v3 (graph/loop/override + `engine`/`model_used`/`verification` + indexes + `(parent_task_id,edge_id)` unique), `outputs` (path-per-row file trees, content via FileStore), `context_files`, `agent_memories`, `orchestrator_messages`, `notifications` v3, `config` v3 (caps, budgets, `tier_models`, `model_pricing`, timeouts). `seed.py` v3: **4 team templates** (Data is P1, D44) with **engine per template** (development & design = `agent_sdk`, planning & research = `crew`, D43) + their **authored role catalogs** (`agent_templates`: role_key, role_instructions, default_tier, optional default edge, `is_starter`) — **prompts authored in `specs/role-catalog.md`** (11 P0 roles) + config maps. Project creation instantiates **one starter agent per team** (the `is_starter` role, D41: Planning→PM, Research→Researcher, Design→Product Designer, Development→SWE — adjustable). **Verify (live):** migration clean on live Postgres; seed idempotent; **4 templates** seeded (no Data); one starter agent per template with correct engine/tier; catalog served by `GET /templates`; one-outgoing-edge index enforced; tier/engine/pricing data exact per spec.

- [x] 6. **Projects API + template cloning** — `GET /templates`; `POST /projects` (transactional clone incl. default edges + tiers); `GET/PATCH/DELETE`; `POST /pause|resume`; `GET /projects/{id}/map` (teams+agents w/ status+tier, edges, paused). TenantScope→project ownership (cross-user 404). **Verify (live):** curl clone from 2 templates → map shows zones/agents/edges/tiers; foreign token 404s; delete cascades.

- [x] 7. **Team/agent/edge management API** — `GET /templates` returns per-team **role catalog** (D41); team add/rename/remove + `PATCH /teams/{id}` `{room_x,room_y}` (D39); agent add (one-modal contract: optional `role_key` prefill + name/**tier**/role/**single output** = handoff|review_loop|none, D38/D41), **5-per-team cap → 409** (D37), edit (incl. tier), remove (cancel tasks, drop edges; **409 while working/queued**); edge create/delete with **one-outgoing-edge + cycle validation** (D25/D38) + loop N validation (1–10); `GET /agents/{id}` / `GET /teams/{id}` panel payloads. **Verify (live):** catalog returned per team; full CRUD; role_key prefill resolves; 6th agent rejected; second outgoing edge rejected; cycle edge rejected; remove-while-working 409s; room position persists; tier change persists and is reflected in map payload.

- [x] 8. **TaskService v3 + dispatch gates** — 7-state transition table; five atomic gates (pause, busy-agent, concurrency cap, daily cost cap from per-model pricing, chain budget); stop semantics (`failed(stopped)` + propagation-suppression flag + engine-aware kill hook); idempotency `(task_id, attempt)`. **Verify:** unit tests for every legal/illegal transition, each gate solo + combined under simulated parallel dispatch, stop from both states.

- [x] 9. **FileStore + context/outputs/memory APIs** — `FileStore` interface + `PostgresFileStore` (D27); context upload (txt/md/pdf allowlist + extraction), list/delete; outputs list (task-grouped trees), preview (text/md/code), per-file download, **`GET /tasks/{id}/outputs.zip`**; memory GET/PUT/DELETE. **Verify (live):** real PDF extracted; multi-file output rows round-trip; zip of a seeded tree downloads and unpacks correctly; bad type rejected.

- [x] 10. **Celery worker + CrewAI runner (text teams)** — engine router skeleton (`crew` path live, `agent_sdk` path stubbed to BLOCKED until item 18); prompt assembly (role+context-budget+memory+input+instructions); dynamic CrewAI agent factory from DB rows with **tier-resolved model**; terminal/awaiting transitions; `result_markdown` + FileStore output; token/cost via `model_used`×pricing + `usage` SSE event; light-tier memory append (isolated failure); reaper beat. **Verify (live):** text task end-to-end with mocked Claude on live Postgres/Redis; tokens/cost recorded per pricing map; reaper fails a stale task; memory appended.

- [x] 11. **GraphEngine** ⭐ — on-`done` propagation transactional with completion: handoff fire (provenance+input_payload), override consumption (D21), pause/stop suppression, chain-budget halt + notification, dedup index. Review-loop protocol (D19): producer→reviewer→(APPROVED early-exit | revision | N-exhausted unapproved report); loop gates downstream. Engine-agnostic (works for stubbed dev tasks too). **Verify:** A→B→C chain hands-off; loop approves early / exhausts N + report; stopped task fires nothing; redelivery doesn't double-fire.

- [x] 12. **Notifications + SSE + board/usage APIs** — project channel `project:{id}`; notification service v3; `GET /sse` (`task_status`/`notification`/`usage` + heartbeats); `GET /board` (goals×tasks projection); `GET /usage` (totals/by_team/by_agent from per-model pricing). **Verify (live):** SSE client receives correct events for a real transition; board mirrors a dispatched goal; usage sums match rows.

- [x] 13. **Orchestrator service** — LiteLLM tool-loop (strong tier, prompt caching): `create_goal`, `dispatch_task(+override_route)`, `get_project_status`, `resume_task` (D22), `list_outputs`; behavior contract; history persistence; `POST /chat` + history. **Verify (live + scripted LLM):** freeform → goal+tasks+accurate reply; "status?" matches DB; chat-resume continues a needs-input task; override routes once without mutating edges.

## Track 2 — Execution engine (parallel after item 5)

- [x] 14. **SandboxProvider + E2B adapter** — interface (create/pause/resume/destroy/exec/read/write/file_tree) + E2B implementation + 2 runtime templates (Node 22 + Playwright, Python 3.12); registry-allowlist egress config; exec timeouts. **Verify (live E2B):** contract test creates sandbox → runs commands → writes/reads files → pause/resume preserves state → destroy; disallowed egress blocked; hung command killed by timeout.

- [x] 15. **WorkspaceService + lifecycle** — `ensure_running` (lazy create per project, resume if paused), `pause_if_idle`, `kill_current`, `destroy`; `projects.sandbox_id/status` bookkeeping; boot/resume failure → clean task failure + `error` status + recreate path. **Verify (live):** first dev task creates, second reuses; idle → paused (E2B billing stops); kill/recreate paths work; project delete destroys sandbox.

- [x] 16. **Agent SDK dev-runner** — per-task fresh Agent SDK session in the workspace: tier-resolved model, bash/file tools scoped to workspace, shared prompt assembly + workspace conventions + sentinel contract (`AWAITING_INPUT`/`APPROVED`), per-command + per-task (30 min default) timeouts, token capture, command log → `tasks.verification`. **Verify (live E2B + real Claude on a tiny task):** "create a Python script that passes its own test" runs end-to-end; needs-input sentinel surfaces; timeout kills cleanly; tokens recorded.

- [x] 17. **Verification toolchain + output collection** — dev-server management (start/health/port) + headless Playwright invocation inside the sandbox; QA-role prompt addendum (APPROVED = verified-by-running, D31); **Design render path: Playwright screenshots the rendered page → PNG outputs** (D42); mtime-diff changed-file collection with ignore rules (node_modules/.next/venv) → outputs rows (incl. PNGs) + zip. **Verify (live):** golden-path test — scaffold 1-page Next.js app → dev server up → Playwright asserts rendered content → outputs tree collected + zip valid; a design task produces a PNG screenshot output; ignore rules hold.

- [x] 18. **Engine integration** — worker routes `agent_sdk` tasks through WorkspaceService+dev-runner (unstub item 10's router); stop kills sandbox command (item 8 hook); pause blocks dev dispatch; GraphEngine cross-engine handoffs (dev↔text) and SWE↔QA review loop over real execution. **Verify (live):** chat-dispatched dev goal runs implement→QA-verify→APPROVED→handoff to text agent; stop mid-execution terminates command + suppresses propagation; full-loop test with mocked Claude + real sandbox.

## Track 1 — Frontend

> **Items 19–26 recreate `claude-design-handoff/product/` (README + `.dc.html` prototypes) in our stack. README = visual source of truth: tokens, Workstation spec, 5-slot system, per-screen breakdown. Behavior conflicts resolve to the PRD (D36).**

- [x] 19. **Frontend setup + design tokens + UI primitives** — Next.js App Router + Clerk (app group gated) + Tailwind; fonts (Baloo 2 / Nunito / JetBrains Mono); encode the handoff tokens as Tailwind theme + CSS vars (**two-layer color rule**, 5 status colors + chip pairs, glass/dark-tile/pill-button/card shadows & radii); shared primitives: `PillButton`, `StatusChip`, `GlassPanel(372)`, `DarkTile`, `Modal`/`Overlay` frame, `Stepper`; a `/design` reference route rendering all primitives. **Verify:** `/design` matches the handoff Design System doc (colors/type/radii/shadows) side-by-side; primitives reused (no ad-hoc styles) downstream.

- [x] 20. **Onboarding wizard (Flow 0)** — 5-step card (680px) on calm gradient+grid, stepper (done/active/future): Google sign-in → display name → project name → **team multi-select** (4 template cards w/ rosters shown for flavor; Data is P1, D44) → optional context dropzone → green "Enter the office →" → `POST /projects` → map. Returning-user path. **Verify (live):** new account lands on a map with exactly the selected teams (one agent each, D37); context file visible in settings; returning user skips.

- [x] 21. **Pixi world: map, rooms, Workstation** — **flat front-view 2D, pan/zoom only (D35)**; MapCanvas (dynamic import); `Room` (wall band + 32px checker floor + team carpet + hanging sign + drop shadow; 3 sizes by headcount; **5-slot placement + occupied-bbox centering** + empty-room hint pill); `Workstation` (106×82 footprint, top-left anchor, mirror facing, monitor **glow** + overhead **badge** screen-space-sized); Camera (pan + wheel-zoom clamp 0.55–1.4 + fit; 4px drag-vs-click threshold; click empty floor closes panels); room drag by wall bar → persist `room_x/y` (D39). **Verify:** renders live `/map`; 5th agent fills last slot then "full"; badges legible at min/max zoom; room drag persists across reload; 60fps sanity.

- [x] 22. **Status pipeline (the visual core)** — single Zustand store: `agents{id→status,tokens}` (snapshot, last-write-wins) + `events[]` (append-only ring); SSE (`task_status`/`notification`/`usage`) updates both; Pixi subscribes imperatively (glow/badge/anim — **`blocked`→`needs-input` visual**, D36); HUD/panels select from same store (chips, header tints); feed/toasts/bell project from `events`; reconnect → re-fetch `/map`+`/usage` to replace snapshot. **Verify:** one simulated transition updates glow + chip + feed + badge identically; no path disagrees; reconnect reconciles snapshot; blocked renders as needs-input.

- [x] 23. **HUD** — OrchestratorChat (bottom-center, **focus-mode bubbles + world dim**); ActivityFeed (top-right dark, colored status word, row→focus); Bell + NotifDrawer (unread, mark-all-read); ToastStack (top-center, View→focus, ×); TokenCounter (bottom-right + per-team popover); ProjectSwitcher (top-left, switch/add→mini-wizard); UtilityStack (Settings/Board/Outputs/+Team); ZoomControl (+/fit/−). **Verify (live):** chat dispatch animates an idle agent + feed streams + counter ticks; bell/toast/feed all focus the right agent; project switch swaps the office.

- [x] 24. **Inspector panels + management modals** — TeamPanel (initial avatar, inline rename, AGENTS/TOKENS tiles, agent rows→agent panel, +Add agent / Rename / Remove); AgentPanel (**status-tinted header**, role inline-edit, tier chip, single connection chip, TOKENS/STATUS tiles, **ProvideInput-when-needs-input**, **ErrorSummary-when-failed**, **Stop-when-working**, Remove **gated** while working/queued); AddAgentModal (**role-catalog picker** prefilling name/role/tier/output, all editable, D41 / **TierPicker** Strong·Medium·Light / role textarea / **OUTPUT segment** Handoff·Loop·Final + target row; "desks are full" banner at 5); AddTeamModal (4 cards, in-office dimmed); Confirms. **Verify (live):** catalog pick prefills then edits; hire end-to-end with tier + single output persisted; 6th hire blocked; stop a running task; provide-input resumes (badge/toast clear); remove gated while working.

- [x] 25. **Overlays** — BoardOverlay (840px, goals→checklist w/ status icons, in-progress/failed rows→focus); SettingsOverlay (860×600, tab rail Context/Memory/Guardrails/Project: context dropzone+list, per-agent memory cards+clear, daily-cost-cap stepper $10–100 + concurrency 1–5 + **Pause project** toggle, rename + delete danger zone); OutputsList (960×600, per-task cards + file tree w/ per-file download + read-only **code/markdown preview + image preview for design screenshots** + **Download zip**). **Verify (live):** board mirrors a dispatched goal + deep-links; pause blocks dispatch from UI; cost/concurrency caps persist; memory edit persists; a dev task's tree previews and zips; a design PNG previews as an image.

- [x] 26. **Focus loop + SSE reconcile + full E2E** — unify the focus action (feed/notif/toast/board → camera tween to center + open panel); reconnect re-fetch. Playwright E2E: sign-in → onboarding → chat dispatch → text teams produce docs → **dev team builds + QA-verifies a mini-app in a real sandbox** → handoff auto-fire → review loop w/ APPROVED → board/outputs(zip)/usage verified → continuation (panel AND chat) → stop + project pause → PRD §11 acceptance criteria walked. **Verify:** E2E passes against the live stack (real E2B; Claude mocked except one golden-path dev run).

- [x] 27. **Landing + admin MDX blog (SEO/GEO)** — `(marketing)` group: SSG landing (intro, CTA→sign-in) + metadata/OG/canonical/sitemap/robots(AI crawlers)/llms.txt/JSON-LD(SoftwareApplication); **MDX blog (D33)** — `content/blog/*.mdx` → `/blog` + `/blog/[slug]` SSG (markdown render, per-post metadata/OG/canonical + **Article JSON-LD**, posts in sitemap, linked from llms.txt); **no admin UI/auth/DB**; CI bundle check (no Pixi in marketing). Built from the sibling `claude-design-handoff/marketing/` handoff when the user picks it up. **Verify:** `curl /` and `curl /blog/<slug>` return full HTML sans JS; sample MDX post renders + in sitemap; robots/llms.txt + AI crawlers allowed; JSON-LD validates; Lighthouse ≥90 Perf+SEO; no Pixi in marketing chunks.

## Phase 2 — Closure (PRD v2.1 §16 / tech-design §22 / D47–D52; appended 2026-07-07)

> **Context:** product is LIVE (billing ON since 2026-06-20) — every item ships via branch → PR → CI → squash-merge → auto-deploy, with billing paths untouched. Build order below is deliberate: the data layer (28) underpins everything; preview backend (29) and result-in-flow (30) are independent and parallelizable; UI (31–32) needs both; QA (34) gates the `preview_enabled` flip. **Feature verification against live infra (real E2B; staging project on prod stack), not just builds.** UX reference: `mockups/mvp-closure-mockup.html`.

- [ ] 28. **Project files + version snapshots (backend, D50)** — migration: `project_files` (project-scoped path manifest → `output_id`) + `workspace_versions` (per-project `version_no` sequence, `manifest` jsonb, `task_id`); worker write path in BOTH engines (CMA `_collect_outputs` + E2B `collect_outputs`): upsert collected files → cut version **in the same transaction as the terminal transition**; `GET /projects/{id}/versions` + `GET /projects/{id}/files?version=`. **Verify (live):** two sequential dev tasks → v1/v2 exist, v2 manifest contains task-1's unchanged files + task-2's changes; crash between collect and commit leaves no orphan version; existing outputs endpoints byte-identical (regression).

- [ ] 29. **Preview Service (backend, D49)** — `PreviewService` (start/status/sync/stop per tech-design §22.2): materialize latest version into an on-demand E2B sandbox, lockfile-cached `npm install`, dev server + health poll, `get_host` URL; `projects.preview_*` columns; lifecycle beat (idle 10 min → pause via `preview_last_active_at`; 24h paused → destroy; 1/project); runnable-target detection (no `package.json` dev script → `none`); `POST /preview/start|stop` + `GET /preview` + SSE `preview_status`; **`preview_enabled` config flag, default OFF in prod**; sandbox-minutes logged. **Verify (live E2B):** real project files → URL serves the app; new version → `sync` reflects change (HMR or restart); idle 10 min → paused (billing stops) → revisit resumes; install failure → `error` + command tail, no task impact; URL absent from logs.

- [ ] 30. **Result-in-flow rendering (frontend, D51)** — AgentPanel renders `result_markdown` on `done` (sanitized markdown, raw-HTML neutralized, lazy-loaded renderer + highlighter kept out of the base office chunk) + files link; Outputs overlay upgrades `<pre>` → rendered markdown / highlighted code. **Verify:** done task shows rendered result in-panel with zero extra clicks; XSS corpus from `ADVERSARIAL_TEST_RESULTS.md` re-run against the new renderers (stored-XSS stays dead); bundle check — no highlighter in the marketing or base office chunks.

- [ ] 31. **Preview card + Theater mode (frontend, D51)** — AgentPanel live-thumbnail card (LIVE dot from `preview_status`, URL row, new-tab, zip; hidden when no runnable target); Theater overlay: large iframe + browser-chrome bar, version chips from `/versions`, **docked orchestrator chat** (same store/history/endpoint as main chat — one conversation, two mounts), open→`preview/start`+heartbeat, ✕/ESC→office intact; store `preview` slice + `theaterOpen`; SSE handlers for `preview_status`. **Verify (live):** card appears only for runnable dev projects; theater cold-open shows "starting…" then the real app; ESC restores the office exactly; iframe is cross-origin-isolated (preview app cannot script the parent).

- [ ] 32. **Iteration loop wiring (D50+D51)** — docked-chat dispatch → dev task against current project state → on `done`: version cut (item 28) → SSE → chips advance + `PreviewService.sync` refreshes the iframe **without manual reload**; chat confirmation message ("applied — preview updated ✓ vN"). **Verify (live, the north-star):** "make the button blue"-class request from the theater lands in the running app end-to-end; a second concurrent text task doesn't cross-talk the preview; main-chat (non-theater) iteration produces the same version/refresh behavior.

- [ ] 33. **Signup credits 240 → 500 (D52)** — `SIGNUP_CREDITS` constant + grant tests updated; decision D46 B-5 note already amended. **Verify:** fresh signup on staging grants 500; existing accounts unchanged; unit suite green.

- [ ] 34. **Phase 2 launch QA gate (blocks `preview_enabled` flip)** —
  - **E2E (Playwright, fresh account):** signup → onboard → first dev task → in-panel result → theater → app serves → docked-chat iteration → chips+iframe update → credits deplete correctly (500 economics) → paywall appears. The PRD §16.3 acceptance criteria walked item by item.
  - **Regression:** Phase 1 core flows (map/panels/board/outputs zip/continuation/stop/pause/project switch) + **billing suite green with zero billing-path diffs** (live money — checkout/webhook/topup/portal spot-check on staging keys).
  - **Adversarial spot-check (new surfaces):** preview URL enumeration/cross-project access, iframe sandbox escape attempt, markdown-renderer XSS corpus, malicious `package.json` dev script (runs only inside the preview sandbox).
  - **Cost/perf:** preview sandbox-minutes logged + idle-pause confirmed in prod; theater open p95 within the bounded startup budget; Lighthouse on marketing routes unchanged.
  - Then: flip `preview_enabled` ON via migration (same pattern as `billing_enabled`), monitor first week (sandbox-minutes, preview errors, funnel: signup → first preview opened).
